/**
 * some 500 ao valor de scroll abaixo
 * atribuindo o novo valor de scroll
 */
var scroll = 1000;
scroll =+ 500;
console.log(scroll);
/**
 * Atribuia true para avariavel darCredito,
 * caso o cliente possua casa e carro.
 * e false caso o contrario
 */
var possuiCarro = true;
var possuiCasa = true;
var darCredito = (possuiCarro && possuiCasa) ? 'Credito aprovado' : 'Credito nao aprovado';
console.log(darCredito)
